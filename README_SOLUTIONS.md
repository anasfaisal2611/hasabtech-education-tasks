# hasabtech-education-tasks — Sample Solutions (Subset)

This ZIP contains **sample implementations** for a subset of tasks:
- `js-basics/tasks/task1.js` — export a `name` variable
- `js-basics/tasks/task2.js` — `add(a,b)`
- `js-basics/tasks/task3.js` — `isEven(n)`
- `js-intermediate/tasks/task1.js` — `sumArray(arr)`
- `js-intermediate/tasks/task2.js` — `reverseString(str)`
- `js-advanced/tasks/task1.js` — `LRUCache` class

> Copy the corresponding files into your original repository at the same paths.
> If your repository’s tests differ, adjust function names/exports accordingly.

## Local testing (optional)

```bash
npm install
npm test
```
